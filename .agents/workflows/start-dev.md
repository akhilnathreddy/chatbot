---
description: Start the Next.js and Express development servers
---

This workflow starts both the frontend and backend servers for the Axon Chatbot project in the background.

// turbo-all
1. Run `npm run dev` in the `/Users/akhilnathreddy.k/Documents/Projects/chatbot/server` directory with a WaitMsBeforeAsync of 5000.
2. Run `npm run dev` in the `/Users/akhilnathreddy.k/Documents/Projects/chatbot/client` directory with a WaitMsBeforeAsync of 5000.
